{
  "name": "ap-bio-practice",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AP Biology question practice with instant MCQ feedback and FRQ self-review",
  "scripts": {
    "start": "node --env-file-if-exists=.env scripts/serve.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check core.js && node --check tutor.js && node --check server/tutor.js && node --check scripts/serve.js"
  },
  "engines": { "node": ">=22.9" }
}
